{% block stylesheets '' %}
    
<div class="tab-container">
    {% block content %}
        {{ "{% block content %}" }}
        {{ "{% endblock content %}" }}
    {% endblock content %}
    <br/>

    <div class="text-right">
        {% block buttons '' %}
    </div>
</div>
<script>
    $(function(){
{#       Twitter boostrap 3 #}
        $("div.navigation > ul > li > a").click(function(e){
            e.preventDefault();
            var href = $(this).attr("href");
            tabsService.loadContent(href);
            return false;
        });
        
{#       Twitter boostrap 4 #}
        $("div.navigation > nav > ul > li > a").click(function(e){
            e.preventDefault();
            var href = $(this).attr("href");
            tabsService.loadContent(href);
            return false;
        });
        {#       Twitter boostrap 3 #}
        $("table.table-paginate > thead > tr:nth-child(1) > th > a").click(function(e){
            e.preventDefault();
            var href = $(this).attr("href");
            tabsService.loadContent(href);
            return false;
        });
    });
</script>
{% block javascript '' %}